# SeismicGuard 工作台优化路线(v0.7.7+)

> 基线:分析内核已基本就位(游标/框选/冻结、P-S 拾取+S-P、物理单位 m/s、
> ML 震级、自由缩放、TauP 理论到时、事件驱动、静态回放、波段选择)。
> 本路线列接下来的方向,来源:`seismic-software-survey.md` 缺口表 +
> `workbench-assessment.md` + 波段/滤波讨论。

## 第一档 — 把分析内核做深 ✅ 全部完成(v0.7.7)
1. ✅ **ZNE→ZRT 旋转**(径向/切向)+ **标签跟随波段**。
2. ✅ **滤波频带预设 + 回放免重下载重滤**。
3. ✅ **完整去仪器响应**:回放输出 Counts/速度/位移/加速度,整段
   `remove_response`(按通道匹配,BH1/BH2 可用),后台+缓存,降级到 Counts。

## 第二档 — 频域与极化 ✅ 完成
4. ✅ **PSD 功率谱(Welch)+ 主频**:回放新增 PSD 面板,框选黄框即算选区频谱
   (功率 dB vs 频率 + 主频标线)。
5. ✅ **从质点运动算极化方位角 + 直线度**(主成分/协方差特征分解),显示在 Hodograma。
   (另:Hodograma 刷新稳定化 — 节流 + 峰值保持量程 + 按实际 dt 取窗。)

## 第三档 — 事件/目录工作流
6. ✅ **事件目录浏览器**:Workbench 新增「事件」标签,USGS 列表可排序,双击进
   回放复核(事件驱动)。对标 SeisAn EEV / scolv。
   - 待办:本地触发录制纳入列表;复核拾取累积成**持久本地 QuakeML 目录**
     (目前已有按事件 QuakeML 导出)。

## 第四档 — 打磨与稳健
7. ✅ 波形标签跟随波段(并入第 1 项)。
7b. ✅ 示波器优化:十字光标吸附数据 + 时间/振幅读数;黄色选区跨三分量同步;
    侧栏频带预设按钮文字不再截断(缩短+Hz 提示)。
8. ✅(部分)频谱图:**dB 色标 + 拖动调对比度**完成;**对数频率轴**待办
   (ImageItem 需对数重分箱);FFT 窗长可调待办(需接 SpectrumComputer)。
9. ✅ 全屏 kiosk 监测模式(F11 切换 / Esc 退出,隐藏头部+标签栏)。
10. **强度卡压成顶栏芯片**(与 Standard 模式共用、风险大 → 单独排期,后续做)。

## 搁置(已论证价值低)
- 多台站对比/定位(IRIS 稀疏)。
- 实时波段选择(BH 够用)。
- 插件系统(架构太重、过早)。

## 独立轨道(发行,非功能)
- v0.7.6.1 发布产物缺失;无自动更新机制。见 CLAUDE.md 已知问题。
